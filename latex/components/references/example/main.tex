\documentclass[english]{../../../unipd-notes}

\unipdsetup{
  course = {Computer Engineering Fundamentals},
  short-course = {Engineering Fundamentals},
  document-type = {Lecture notes}
}

\begin{document}
\chapter{Interprocess communication}
\section{Producer--consumer buffer}\label{sec:elements}

\begin{theorem}\label{thm:identity}
If access to the buffer is protected by mutual exclusion, the number of elements remains between zero and the capacity $N$.
\end{theorem}

\begin{equation}\label{eq:identity}
  0 \leq n_{\mathrm{buffer}} \leq N
\end{equation}

\begin{figure}[htbp]
  \centering
  \begin{tikzpicture}[node distance=16mm]
    \node[draw=unipd-primary,rounded corners] (source) {Producer};
    \node[draw=unipd-primary,rounded corners,right=of source] (target) {Consumer};
    \draw[draw=unipd-accent,-{Latex},thick] (source) -- (target);
  \end{tikzpicture}
  \caption{Data flow through the shared buffer}
  \label{fig:reference}
\end{figure}

\begin{table}[htbp]
  \centering
  \caption{Buffer semaphore state}
  \label{tab:types}
  \begin{tabular}{ll}
    \toprule
    Semaphore & Initial value \\
    \midrule
    \texttt{mutex} & $1$ \\
    \texttt{empty} & $N$ \\
    \texttt{full} & $0$ \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{code}[language=Python,caption={Checking buffer availability},label={lst:identity}]
def can_consume(items):
    return items > 0
\end{code}

\begin{algorithm}[htbp]
  \caption{Removing an element from the buffer}
  \label{alg:identity}
  \KwInput{A non-empty shared buffer}
  \KwOutput{The oldest element}
  wait(\texttt{full})\;
  wait(\texttt{mutex})\;
  $x\gets$ remove element\;
  signal(\texttt{mutex})\;
  signal(\texttt{empty})\;
  \Return{$x$}\;
\end{algorithm}

The invariant in \cref{thm:identity}, expressed by \cref{eq:identity}, prevents overflow and underflow. \Cref{fig:reference} summarizes the flow, while \cref{tab:types,lst:identity,alg:identity} describe initialization and removal.
\end{document}
